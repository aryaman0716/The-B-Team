using UnityEngine;

public class PropHolder : MonoBehaviour
{
    public static PropHolder Instance { get; private set; }

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
        }
        else
        {
            Destroy(this);
        }
    }
}
